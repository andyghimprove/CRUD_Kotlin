type Mutation {
    createNewClient(newClient: ClientInput): ClientModel
    updateClient(id: Int!, updateClient: ClientInput): ClientModel
}

input ClientInput {
    name: String!
    last_name: String!
    email: String!
    payment_amount: Int!
}



